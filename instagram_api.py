import requests

def post_to_instagram(image_path, caption, access_token, instagram_account_id):
    """
    Simuleert het plaatsen van een post via Instagram Graph API.
    In productie: vervang dit met echte stappen voor media upload + publish.
    """
    print(f"📸 Echte post naar Instagram: {image_path} | Caption: {caption}")
    return True
